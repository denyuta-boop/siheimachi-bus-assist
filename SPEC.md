# sendai-bus-assist

仙台市営バス「子平町・壽徳寺前」から「仙台駅」への2系統について、
現在時刻からどちらに乗るべきかを即座に判定・表示するモバイルファーストのPWA。

## 背景・目的

- 対象停留所「子平町・壽徳寺前」からは、仙台駅行きが2系統ある
  - （01）晩翠通経由：01番のりば・南向き、標準所要時間 約25分
  - （02）県庁市役所経由：02番のりば・北向き、標準所要時間 約33分
- 所要時間差は約7〜8分あるため、どちらに乗るべきかは「今どちらが先に来るか」で毎回変わる
- 毎回時刻表を見比べるのが面倒なので、開いた瞬間に結論が出るアプリにする

## 技術スタック

- Vite + React + TypeScript + Tailwind CSS
- PWA対応（vite-plugin-pwa）
- ホスティング：Vercel

## 機能要件

### 必須（v1）

- [x] 現在時刻から各系統の次発便を取得
- [x] 仙台駅到着予定時刻（発車時刻＋標準所要時間）を計算
- [x] 到着が早い方を「おすすめ」として画面上部にハイライト表示
- [x] 各系統ごとに「次発」に加えて「逃した場合の次の便」を表示
- [x] 開閉式の時刻表ビュー（両系統の全便一覧、過去はグレーアウト、次発をハイライト）
- [x] のりば番号・方向（01番のりば南向き／02番のりば北向き）を明記
- [x] スマホ表示前提のUI、PWA化してホーム画面に追加可能にする

### 今後の拡張（v2以降・優先度低）

- [ ] 平日／土曜／休日でダイヤを自動切り替え（`Date.getDay()`で判定）
- [ ] GTFS-RT等のリアルタイム運行情報と連携し、遅延を加味した判定に対応
- [ ] 実際のGTFS-JPデータへの差し替え（現在はモックデータ）

## データ仕様

時刻表データは `src/data/timetable.ts` 等に分離し、以下の構造を持たせる：

```ts
type Route = {
  id: "01" | "02";
  label: string;       // 例: "晩翠通経由"
  sub: string;          // 補足説明
  platform: string;     // 例: "01番のりば"
  direction: string;    // 例: "南向き"
  duration: number;     // 分単位の標準所要時間
  times: number[];      // 発車時刻（0:00からの分数の配列）
};
```

モックデータは平日日中ダイヤを想定した仮の等間隔ダイヤで良い。
実データは仙台市交通局公式サイトまたはGTFS-JPから後日差し替える。

## 判定ロジック

1. 現在時刻以降で各系統の直近の発車時刻を取得
2. 到着予定 = 発車時刻 + 標準所要時間
3. 到着予定が早い系統を「おすすめ」とする
4. 各系統について、次発の次（2件目）の便も保持し「逃した場合」として表示する

## 非機能要件

- 個人情報は一切扱わない（Public/Privateどちらでのホスティングでも問題ない想定）
- オフラインでも起動できること（静的データのみで判定可能なため）
- ダイヤ改正時にデータ更新のみで追従できる構造にする

## 進捗ログ

<!-- Claude Codeが作業するたびにここに追記していく -->

- 2026-08-16: 仕様書作成、開発開始
- 2026-08-16: v1実装完了
  - Vite + React + TypeScript + Tailwind CSS + vite-plugin-pwa でプロジェクト初期化
  - `src/data/timetable.ts`: Route型定義とモックダイヤデータ（01・02系統）を作成
  - `src/lib/schedule.ts`: 次発便取得・到着予定計算・おすすめ系統判定ロジックを実装
  - `src/components/RecommendBanner.tsx`: おすすめ系統をハイライト表示するバナーを実装
  - `src/components/RouteCard.tsx`: 各系統の次発・次の次の便を表示するカードを実装
  - `src/components/TimetableView.tsx`: 開閉式時刻表（過去グレーアウト・次発ハイライト）を実装
  - `src/App.tsx`: 1秒更新のリアルタイム時計と全コンポーネントを統合
  - PWAマニフェスト・Service Worker（Workbox）設定、オフライン動作対応
  - `index.html`: 日本語化・PWA metaタグ追加
