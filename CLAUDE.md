# CLAUDE.md

このファイルはClaude Codeが毎回のセッション開始時に自動で読み込む前提ファイルです。
仕様の詳細は `SPEC.md` を参照してください。ここには「毎回守ってほしいルール」だけを書きます。

## プロジェクト概要

仙台市営バスの2系統（子平町・壽徳寺前 → 仙台駅）のうち、現在時刻から見てどちらに
乗るべきかを判定・表示するモバイルファーストのPWA。個人利用ツール。

## 技術スタック（固定）

- Vite + React + TypeScript
- Tailwind CSS（コアユーティリティクラスのみ。任意値記法 `w-[300px]` のような
  カスタムクラスは使わない）
- vite-plugin-pwa
- パッケージマネージャは npm を使う（yarn/pnpmには変えない）

## ディレクトリ構成

```
src/
  data/
    timetable.ts     # 時刻表データ（Route型、モック/実データ）
  components/
    BusCard.tsx       # 系統ごとのカード（次発＋逃した時の代替）
    TimetableView.tsx # 開閉式の全便一覧
  lib/
    schedule.ts       # 判定ロジック（次発計算・到着予測・比較）
  App.tsx
```

新しいロジックを追加する時は `lib/` に関数として切り出し、コンポーネント内に
直接計算ロジックを書かない。

## コーディング規約

- コンポーネントは関数コンポーネント＋TypeScriptの型を必ず明示する
- 時刻はすべて「0:00からの分数（number）」で内部計算し、表示直前だけ `HH:MM` 形式に
  変換する（`toClock()`のような変換関数を1箇所にまとめる）
- マジックナンバー（所要時間25分・33分など）は `data/timetable.ts` の定義から
  参照する。コンポーネント内にハードコードしない
- スタイルは既存のダーク配色（ネイビー背景＋アンバー/ティール系アクセント）を踏襲する。
  新しい配色を追加する時は理由をコメントで残す
- コミットメッセージは日本語で、変更内容が一目でわかる一文にする
  （例: `時刻表の平日/休日切り替えを追加`）

## 作業の進め方

- 実装を始める前に `SPEC.md` を読み、対応するチェックボックスの項目を確認する
- 機能を実装し終えたら `SPEC.md` の該当チェックボックスを `[x]` にし、
  「進捗ログ」に日付と内容を1行追記する
- 仕様に書かれていない判断が必要な場面では、作業を止めずに妥当な形で実装し、
  何を判断したかを進捗ログかコミットメッセージに残す
- `npm run build` がエラーなく通ることを、区切りの良いタイミングで確認する

## やらないこと

- リアルタイムAPI（ODPT等）との連携は v1 の対象外。SPEC.mdのv2セクション参照
- 個人情報を扱う機能（ログイン、位置情報の外部送信など）は追加しない
- バックエンドサーバーは立てない（静的ホスティングのみで完結させる）
